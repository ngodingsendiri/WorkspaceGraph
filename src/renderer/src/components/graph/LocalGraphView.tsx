/**
 * Local graph under editor — Phase 1 visual canvas (Obsidian-style).
 * Implementation lives in LocalGraphCanvas; this file keeps the old import path.
 */
export { LocalGraphCanvas as LocalGraphView, LocalGraphCanvas } from './LocalGraphCanvas'
