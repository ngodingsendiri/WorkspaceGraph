# TODO — Masalah yang Harus Diselesaikan ke Depan

**Dibuat:** 2026-08-10
**Sumber:** `docs/AUDIT_2026-08-10_save-stop.md` (audit mendalam commit `8de0361` — provider save/test + two-step stop)
**Status umum:** ⏳ Terbuka — daftar kerja; centang setiap item saat selesai.

> Cara pakai: setiap item punya prioritas, file yang terlibat, masalah, solusi yang disarankan, dan estimasi. Centang `[x]` + isi tanggal saat selesai. Kalau ada item yang diputuskan "tidak jadi diperbaiki", tulis alasannya di kolom catatan — jangan diam saja.

---

## P1 — Harus segera (bug UX nyata / alur rusak)

### [x] MED-1 — Pesan "…disimpan — tes otomatis…" tidak pernah tampil di jalur Save dengan key
- **File:** `src/renderer/src/components/settings/SettingsView.tsx` (`handleSaveProviderRow`, `autoVerifyProvider`, `handleTest`, `flash`)
- **Masalah:** `flash()` bersifat replace; `handleTest` memanggil `flash('Testing…')` sinkron sebelum await pertama → pesan "disimpan" tertimpa di batch render yang sama. Kontrak pure-builder benar, tapi pesannya tak pernah dirender.
- **Solusi:** jalankan `autoVerifyProvider` lebih dulu dengan mode silent, lalu `flash(buildRowSaveFlash(...))` setelah verifikasi selesai. Sinkronkan assertion `sourceContracts.test.ts` dengan urutan baru.
- **Effort:** kecil (≤ 30 mnt) · **Status:** ✅ 2026-08-10 (M1)
- **Component test (Testing Library, 2026-08-10):** `SettingsView.test.tsx` (jsdom per-file via `// @vitest-environment jsdom`). Tiga test: (1) MED-1 — auto-verify Save SILENT: deferred ping ditahan, asersi **tidak ada banner "Testing…"/"OK…" saat ping in-flight** (flash() replace → asersi badge akhir saja TIDAK diskriminatif; diuji empiris: mem-break `if (!silent)` membuat test GAGAL); (2) positive control — klik Test manual memang mem-flash "OK openai: connected" (membuktikan asersi silent bukan vacuous); (3) LOW-1 — lihat entry LOW-1. Infra baru: devDeps `jsdom` + `@testing-library/{react,user-event,dom}`; `vitest.config.ts` include `src/**/*.test.{ts,tsx}` (sebelumnya hanya `.ts`).

### [x] MED-3 — Gagal-simpan key di form tambah provider membuang key; pesan "buka kartu & Save untuk ulang" menyesatkan
- **File:** `src/renderer/src/components/settings/SettingsView.tsx` (`handleAddProvider`)
- **Masalah:** `providerDraft` dikosongkan saat configure gagal, dan `apiKeys[finalId]` tak pernah diisi form tambah → Save di kartu tidak punya key; pengguna harus ketik ulang.
- **Solusi:** sebelum menutup form, `setApiKeys((prev) => ({ ...prev, [finalId]: providerDraft.apiKey.trim() }))`.
- **Effort:** kecil (≤ 30 mnt) · **Status:** ✅ 2026-08-10 (M1)

---

## P1 — Keputusan desain (butuh keputusan produk/user)

### [x] MED-2 — Override Test tidak pernah di-revert: key belum-disimpan jadi AKTIF di memori (ghost key)
- **File:** `src/main/ipc/handlers/ai.ts` (`ai:testProvider`), `src/main/ipc/shared.ts` (`loadSettingsIntoProviders`)
- **Masalah:** setelah paste→Test, key aktif untuk chat sungguhan; jika Save tanpa key, kartu tampil "configured" padahal tak ada yang tersimpan (sampai restart/save key baru).
- **Pilihan solusi (pilih satu):**
  - **(a) Revert setelah ping** — snapshot config, restore di `finally`. Trade-off: daftar model pasca-Test memakai key lama.
  - **(b) Pertahankan + komunikasikan** — flash/catatan "key aktif — belum disimpan, Save untuk menyimpan".
  - **(c)** Reset apiKey runtime provider saat `handleSaveProviderRow` tanpa key (perlu flag eksplisit; bentrok dengan guard di `loadSettingsIntoProviders`).
- **Effort:** sedang (1–3 jam) · **Status:** ✅ 2026-08-10 — keputusan (b)+(c), dikerjakan M1
- **KEPUTUSAN (2026-08-10):** **(b) + (c)** — pertahankan override (paste→Test→chat tetap intuitif), TAMPILKAN indikator jujur di kartu saat key terketik belum di-save, dan Save tanpa key me-reset override runtime ke key tersimpan (tutup jalur ghost). Detail teknis saat M1: `ai:testProvider` return `patched:true` → renderer track set id; `handleSaveProviderRow` tanpa key → restore key tersimpan via main (bukan hanya `persistDefs`). **Temuan M1: (c) ternyata sudah ter-cover arsitektur** — tiap row-Save memanggil `ai:saveProviderConfigs` → `rebuildProviders` membuat instance provider BARU tanpa key, lalu `loadSettingsIntoProviders` hanya menerapkan key yang tersimpan. Jadi override Test otomatis hilang setelah Save apa pun. Implementasi M1 = label jujur (b) di kartu ("key baru — belum disimpan") + verifikasi via test; tidak perlu IPC baru.

---

## P2 — Aksesibilitas & perilaku halus (sebaiknya)

### [x] LOW-2 — Stop gate tidak punya jalan keluar: Esc / klik-luar tidak me-disarm
- **File:** `src/renderer/src/components/chat/ChatPanel.tsx` (+ `chatCancelConfirm.ts` jika kontraknya berubah)
- **Solusi:** tambah Esc-to-disarm mengikuti pola yang sudah ada (picker line 464, menu ⋮ line 518). Jangan ubah kontrak klik kedua = cancel.
- **Effort:** kecil · **Status:** ✅ 2026-08-10 (M4) — `useEffect` pada `stopArmed` mendengar `Escape` → `disarmStop()` (pola sama dgn picker/menu); kontrak klik kedua = cancel tidak berubah.

### [x] LOW-3 — `aria-live` countdown berisik untuk screen reader
- **File:** `src/renderer/src/components/chat/ChatPanel.tsx`
- **Solusi:** umumkan sekali saat arm ("Stop — klik lagi dalam 3 detik untuk berhenti"), update visual tetap diam.
- **Effort:** kecil · **Status:** ✅ 2026-08-10 (M4) — label tick (`chat-stop-label`) kehilangan `aria-live`; teks arm diumumkan SEKALI via span `.sr-only role=status` (util `.sr-only` baru di globals.css).

### [x] LOW-1 — Test mengabaikan baseUrl yang diedit-belum-disimpan (tanpa key baru)
- **File:** `src/renderer/src/components/settings/SettingsView.tsx` (`handleTest`)
- **Solusi:** kirim override `baseUrl` juga saat berbeda dari versi tersimpan (perlu snapshot def saat `loadAll`), atau selalu sertakan `baseUrl` pada override.
- **Effort:** sedang · **Status:** ✅ 2026-08-10 (M6) — override **selalu** dikirim: `{ apiKey: typedKey || undefined, baseUrl: def?.baseUrl || undefined }` (tanpa ternary `typedKey ? … : undefined`). Side main `ai.ts`: patch `configureProvider` kini menembak untuk override baseUrl-only; kegagalan patch dikembalikan sebagai `configureError` terpisah (bukan label error test generik). Contract sourceContracts di-update (token baru; ternary lama dilarang).
- **Component test (Testing Library, 2026-08-10):** di `SettingsView.test.tsx` — edit baseUrl di kartu (tanpa save) → klik Test → `window.api.testAIProvider` dipanggil dengan `baseUrl` EDITED (`https://edited.example/v2`, bukan `https://saved.example/v1` tersimpan) + `apiKey: undefined`. Render SettingsView asli di jsdom dengan mock `window.api` penuh (proxy fallback untuk method tak terdaftar); `matchMedia` di-stub.

### [x] LOW-6 — `catch` di `ai:testProvider` mencampur error configure dengan error test
- **File:** `src/main/ipc/handlers/ai.ts`
- **Solusi:** pisahkan blok try: patch vs ping.
- **Effort:** kecil · **Status:** ✅ 2026-08-10 (M1)

---

## P3 — Kebersihan & ketahanan test (kapan saja)

### [x] LOW-4 — sourceContracts test terikat kata-kata komentar
- **File:** `src/main/qa/sourceContracts.test.ts`
- **Solusi:** kontrak difokuskan pada token kode; cek komentar dihapus atau dibuat toleran.
- **Effort:** kecil · **Status:** ✅ 2026-08-10 (M4) — assertion `intentionally NOT cleared` (teks komentar) diganti token kode `setApiKeys((prev)`; kontrak MED-1 di-update ke `buildRowSaveFlash(def, true, cfg)` (NIT-1).

### [x] LOW-5 — Label pesan inkonsisten + logika label diduplikasi
- **File:** `SettingsView.tsx`, `providerSaveKeyConfirm.ts`
- **Solusi:** satu helper label bersama (`def.name?.trim() ? … : def.id`).
- **Effort:** kecil · **Status:** ✅ 2026-08-10 (M4) — `providerLabel(def)` diekspor dari `providerSaveKeyConfirm.ts`, dipakai `buildRowSaveFlash` + aria-label nama + title hapus di SettingsView.

### [x] NIT-1 — `{ ok: true }` literal vs `cfg` di `handleSaveProviderRow`
- **Effort:** sangat kecil · **Status:** ✅ 2026-08-10 (M4) — `cfg` di-hoist ke scope fungsi, branch sukses flash `buildRowSaveFlash(def, true, cfg)` (hasil configure asli), bukan literal `{ ok: true }`. Kontrak sourceContracts di-update.

### [x] NIT-2 — `cancelConfirmLabel(0)` → "Stop? 1" transien
- **Effort:** sangat kecil · **Status:** ✅ 2026-08-10 (M4) — `remainingMs <= 0` → `'Stop?'` polos (tanpa angka menyesatkan); unit test.

---

## Non-kode (tindak lanjut)

- [x] **MED-2:** ambil keputusan desain (a/b/c) — **diputuskan 2026-08-10: (b)+(c)** — lihat section P1. (WC-4 → (a); AG-2 → (a) — sudah dicatat di section masing-masing. WB-12 & AD-2 masih terbuka.)
- [ ] **Verifikasi visual:** pastikan ukuran tombol stop vs tombol kirim tidak menggeser baris composer (layout shift).
- [ ] **Rencana:** setelah P1 selesai, pertimbangkan test integrasi kecil untuk alur flash (urutan pesan) — area yang tidak terjangkau unit test murni.

---

## Wave A — Temuan Audit Fondasi (2026-08-10)

> Hasil audit AUD-01..04 (`WorkspaceEngine`, `MarkdownEngine`, `FileWatcher`, persistence/settings). Sumber: `docs/AUDIT_PLAN.md`. Tidak ada temuan HIGH (keamanan/XSS terverifikasi bersih). Ada **1 risiko crash (WA-1)** dan **1 konsistensi data (WA-2)** yang sebaiknya dikerjakan duluan.

### P1 — Risiko crash

- [x] **WA-1 — FileWatcher `emit('error')` tanpa listener → uncaught exception → crash main process.**
  - **File:** `src/main/engine/FileWatcher.ts` (line ±60), `src/main/ipc/shared.ts` (`attachFileWatcher`)
  - **Masalah:** chokidar error → `this.emit('error', err)`; `attachFileWatcher` hanya subscribe `'change'`. EventEmitter yang `emit('error')` tanpa listener melempar exception tak tertangani.
  - **Solusi:** pasang listener `'error'` default (log saja) di `attachFileWatcher`, atau jangan re-emit — cukup `console.error`.
  - **Effort:** sangat kecil

### P2 — Konsistensi data & konten

- [x] **WA-2 — `unlinkDir` tidak cascade: node anak tetap tersisa di graph/search/embedding setelah folder dihapus.**
  - **File:** `src/main/ipc/shared.ts` (`handleFileRemove`, `attachFileWatcher`), `src/main/ipc/handlers/files.ts` (`file:delete`), `src/main/engine/GraphEngine.ts`
  - **Masalah:** hapus folder → event `unlinkDir` → `handleFileRemove(dir)` hanya cari node untuk path folder (tidak ada) → anak-anak tidak dihapus. Stale sampai refresh penuh.
  - **Solusi:** saat `unlinkDir`, hapus semua node yang path-nya berawalan folder itu (graph + search index + embedding).
  - **Effort:** sedang · **Status:** ✅ 2026-08-10 (M2) — `handleDirRemove` + `GraphEngine.removeNodesUnderPath` (1 pass + 1 re-resolve, bukan N×removeNode); `file:delete` folder juga cascade; unit test WA-2.

- [x] **WA-3 — `updateLinksInVault` menulis ulang `[[link]]` di dalam code fence → korupsi contoh kode saat rename file.**
  - **File:** `src/main/engine/WorkspaceEngine.ts` + `src/main/engine/markdownCode.ts` (tokenizer baru)
  - **Masalah:** rename `Note.md` → semua `[[Note]]` di vault diganti, termasuk di dalam fenced/inline code (MarkdownEngine sudah strip code untuk ekstraksi link, tapi rewrite rename tidak).
  - **Solusi:** strip code region sebelum match (reuse pola `stripCodeRegions`).
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M2) — `rewriteWikiLinksOutsideCode` (pure, ter-test) lewati fence ```/~~~ + inline code; golden test.

- [x] **WA-4 — `extractInlineTags` memanen `#tag` di dalam code fence → tag node hantu di graph.**
  - **File:** `src/main/engine/MarkdownEngine.ts`
  - **Solusi:** strip code sebelum scan tag (helper yang sama dengan ekstraksi wikilink).
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M2) — pakai `stripCodeRegions` dari tokenizer (fence ``` + ~~~); unit test.

- [x] **WA-5 — `extractHeadings` menangkap heading di dalam code fence → outline palsu.**
  - **File:** `src/main/engine/MarkdownEngine.ts`
  - **Solusi:** strip code sebelum scan heading (satu helper dengan WA-4).
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M2) — walker line-by-line dengan fence-tracking (``` dan ~~~ tidak saling menutup); unit test.

- [x] **WA-6 — Placeholder `§§WIKIn§§` / `§§EXTn§§` / `§§CODEn§§` bentrok dengan teks literal user (termasuk di dalam `<pre>`).**
  - **File:** `src/main/engine/MarkdownEngine.ts` (`renderMarkdownToHtml`)
  - **Masalah:** user menulis `§§WIKI0§§` → saat restore, teks itu diganti slot wikilink pertama → render salah (bisa merusak isi code block).
  - **Solusi:** token yang tidak mudah bertabrakan + hanya restore di luar `<pre>`/`<code>`, atau escape kemunculan literal placeholder pada input.
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M2) — escape `§` → PUA `\uE000` di input, restore di akhir setelah semua slot; `\uE000` literal user digeser ke `\uE001` (round-trip penuh). *Catatan: pendekatan doubling `§§`→`§§§§` ditolak karena regex restore `§§WIKI(\d+)§§` tetap cocok di dalam hasil doubling.* Unit test.

### P3 / Nit

- [x] **WA-7 — `renameFile` meng-update link SEBELUM rename di disk; jika rename gagal (lock/permission) → link sudah berubah, file belum (dangling).** — Solusi: rename dulu, baru update link; toleransi kegagalan tulis link. ✅ 2026-08-10 (M2) — rename dulu, lalu `updateLinksInVault` best-effort (gagal → log, bukan gagalkan operasi); test: rename ke target ter-lock → throw, semua link tetap utuh.
- [x] **WA-8 — `decryptSecret` mengembalikan `''` diam-diam saat safeStorage tidak tersedia → key tampak hilang tanpa peringatan.** — Solusi: log warning / status eksplisit di `security:status`. ✅ 2026-08-10 (M2) — `decryptSecret` warn sekali per stored value; `security:status` laporkan `decrypt-failed` untuk enc:v1 yang tak bisa didekripsi.
- [x] **WA-9 — `workspace.json` ditulis non-atomic di `openWorkspace`** — pakai `atomicWriteJson` (sama seperti settings/recent). ✅ 2026-08-10 (M1)
- [x] **WA-10 — Fence `~~~` (tilde) belum didukung renderer** — GFM mengizinkan keduanya (``` dan ~~~). ✅ 2026-08-10 (M2) — regex fence pakai backreference `\1` (```/~~~); tokenizer + renderer + `isGfmTableRow`; unit test (render, wikilink di dalam fence tilde tetap literal).
- [x] **WA-11 — `buildFrontmatterString` / `createNoteTemplate` interpolasi title mentah → YAML rusak jika title berisi `:` / newline.** — Solusi: quote/escape nilai frontmatter. ✅ 2026-08-10 (M2) — helper `yamlScalar` (plain-safe → unquoted; metachar/quote/newline → JSON.stringify); H1 di template ratakan newline; unit test + round-trip gray-matter.

### Positif terverifikasi (tanpa aksi)

- XSS renderer Markdown **terkendali** — semua konten user di-`escapeHtml`, URL dibatasi `http(s)/mailto`, `javascript:` jatuh ke teks ter-escape, `data-target`/`align` aman.
- **Tidak ada handler yang mengirim key terdekripsi ke renderer** — semua call site `getSettings()` diperiksa (readPermissions/trashEnabled/status saja; `settings:get` scrub; `security:status` pakai raw terenkripsi).
- `mergeSettingsPreservingSecrets` + `scrubSettingsSecrets` round-trip aman; `saveSettings` atomic (.tmp + rename); corrupt `settings.json`/`recent.json` di-quarantine.
- `readProviderDefs` toleran terhadap `aiProviders` non-array → migrasi v0 (objek `{}`) aman.
- Tidak ada field secret lain di luar `settings.ai` (mcp/plugin/automation bersih).
- Trash solid: validasi path, suffix collision, prune empty dir, emptyTrash count.
- `.trash`/dotfiles/`node_modules` di-ignore watcher; `awaitWriteFinish` 300ms; self-write echo guard 2,5 dtk OK.

---

## Wave B — Temuan Audit Data & Pencarian (2026-08-10)

> Hasil audit AUD-05..08 (`IndexDatabase`, `SearchEngine`, `GraphEngine` + `GraphLayoutStore`, `TemplateEngine`/`DomainEngine`). Sumber: `docs/AUDIT_PLAN.md`. Tidak ada temuan HIGH; mayoritas LOW (skala & konsistensi). Pola yang berulang: **scan/rebuild penuh per-edit** (S-2, G-2, D-2) dan **cap 2000 baris** di tag search (I-1/S-3).

### P2

- [x] **WB-1 — `searchByTag` hanya memindai 2000 catatan terbaru → tag match di luar itu tidak pernah ditemukan.**
  - **File:** `src/main/engine/IndexDatabase.ts` (`searchByTag`), `SearchEngine.ts` (`searchByTagExact` memakainya)
  - **Masalah:** `ORDER BY updated_at DESC LIMIT 2000` lalu filter di JS. Di vault 3000+ catatan (ukuran stress test), pencarian tag diam-diam kehilangan hasil di luar 2000 terbaru.
  - **Solusi:** query berbasis kolom (tabel tag terpisah atau kolom `tags` dengan `LIKE`) — bukan scan terbatas di JS.
  - **Effort:** sedang · **Status:** ✅ 2026-08-10 (M2) — tabel `note_tags` (tag lowercase, PK note_id+tag, index tag) di-populate upsert/clear/remove; backfill JS sekali saat open untuk DB lama; `searchByTag` = JOIN `note_tags` tanpa cap. Test: fake-executor memvalidasi bentuk query (JOIN, tanpa LIMIT 2000) + mapping. *Catatan: harness test global memakai mock better-sqlite3 (SQL tak dieksekusi nyata) — validasi via fake executor; perlu smoke-test manual sekali di vault nyata.*

- [x] **WB-2 — `addToIndex` me-rebuild penuh indeks Fuse worker di SETIAP perubahan file.**
  - **File:** `src/main/engine/SearchEngine.ts` (`addToIndex` → `rebuildWorkerIndex`)
  - **Masalah:** tiap autosave/syncSingleFile mengirim SELURUH index ke worker (O(N) per edit). Operasi batch (impor banyak file) memicu rebuild berulang.
  - **Solusi:** debounce rebuild (mis. 500ms–1s) atau delta update ke worker.
  - **Effort:** sedang · **Status:** ✅ 2026-08-10 (M3) — delta update: tipe pesan baru `updateEntries`/`removeEntries` (worker `fuse.remove(predicate)` + `fuse.add`), main menahan pending map + debounce 250ms (`scheduleFuseFlush`), `search()` mem-flush pending sebelum query worker (tak ada hasil basi). Mode lokal (test/no-worker) terapkan delta sinkron. Test: fake-worker verifikasi batch + flush-before-search.

- [x] **WB-3 — `updateNodeAndEdges` me-rebuild SEMUA edge wiki dari outLinks tiap update satu file.**
  - **File:** `src/main/engine/GraphEngine.ts`
  - **Masalah:** `rebuildWikiEdgesFromOutLinks()` O(N×L) penuh di setiap perubahan — pola sama dengan WB-2 (scaling batch).
  - **Solusi:** rebuild lokal: hanya hitung ulang edge node yang berubah + re-resolve target yang terpengaruh (rename/delete).
  - **Effort:** besar (perlu desain) — bisa jadi satu paket dengan WB-2 · **Status:** ✅ 2026-08-10 (M3, + M5) — inkremental: `rebuildWikiEdgesForNodes` (hanya node tersentuh + node yang link-nya menyentuh old/new `identityKeys` saat add/rename/delete) + `pruneOrphanGhostNodes` + `setsEqual` guard (content-edit murni = TANPA scan O(N×L)). `removeNode`/`removeNodesUnderPath` juga inkremental. Lookup maps tetap O(N) sekali per op. Test: removeNode → ghost; rename flow → ghost old + backlink baru; backlink preserved (deep). **M5 — tag edges inkremental (inverted index):** field `tagIndex` (tagLower → note ids) + `dirtyTags` + `tagEdgeIdsByTag` (ownership star edge per tag); `syncNodeTags(node, remove)` menjaga index per mutasi; `rebuildDirtyTagEdges()` hanya membangun ulang star edge untuk tag yang berubah (tanpa scan semua node); `rebuildTagIndex()` full hanya di `buildFromParsedFiles`; `rebuildTagNodes` tidak lagi create-then-wipe tagnode edges saat co-tag ON (net state sama dengan pra-M5). Kasus edge pair yang sama di dua tag ditangani via transfer kepemilikan `pairConnectedByOtherTag`. 3 test baru (tag change re-hub, removeNode re-hub, cascade folder).

- [x] **WB-4 — `getHubNodeIds`/`getHubNodes` memakai `n.degree` yang MENCAMPUR edge tag.**
  - **File:** `src/main/engine/GraphEngine.ts` (`recomputeDegrees` menghitung wiki+tag; `getHubNodeIds` mengklaim "wiki degree")
  - **Masalah:** hub bisa terinflasi oleh tag hub (#tag dipakai banyak note) → threshold hub tidak akurat.
  - **Solusi:** hitung degree wiki-only untuk deteksi hub (pisahkan counter).
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M2) — `GraphNode.wikiDegree` dihitung dari edge wiki_link saja; `getHubNodeIds/getHubNodes` pakai wikiDegree (fallback degree utk node lama). Unit test: co-tag star edges ON, tag hub degree ≥5 tapi tidak jadi hub.

### P3 / Nit

- [x] **WB-5 — Duplikasi ±150 baris antara `searchSync` dan `search` (+ preview logic di `searchFuseWorker`)** — risiko drift saat salah satu jalur diubah. Ekstrak core bersama. · **Status:** ✅ 2026-08-10 (M3) — core bersama: `keywordResults` (FTS+Fuse merge + filter), `specialSearch` (orphan/backlink/path/#tag), `fuseHitsToResults` (preview + matchedField, dipakai searchSync & fallback lokal worker). Satu-satunya perbedaan tersisa: sumber fuse hits (lokal sync vs worker async) + hybrid semantic (async).
- [x] **WB-6 — `getTemplate`/`renderById` → `listTemplates` membaca direktori + SEMUA file Templates tiap panggilan** (tanpa cache). Cache dengan invalidasi mtime. · **Status:** ✅ 2026-08-10 (M3) — cache per-workspace: signature = nama+mtimeMs+size tiap file `.md` di `Templates/`; edit/tambah/hapus file meng-invalidasi, panggilan berulang (list, renderById, konteks AI) tidak baca ulang. Test: cache hit (reference sama), invalidasi edit & tambah file.
- [x] **WB-7 — `parseCheckboxes` tidak mencocokkan `+ [ ]` (GFM mengizinkan `+`), hanya `-` dan `*`.** ✅ 2026-08-10 (M2) — regex `[-*+]`; unit test.
- [x] **WB-8 — `getOverview()` DomainEngine menghitung ulang penuh tiap panggilan** (dashboard + listByType + peopleLinkedTo masing-masing memanggil) — cache sampai `setParsedFiles`. · **Status:** ✅ 2026-08-10 (M3) — cache `overviewCache` di `getOverview`, invalidasi di `setParsedFiles`/`clear`; `listByType`/`peopleLinkedTo` dilayani dari cache yang sama. Test: hit (reference sama) + invalidasi. *Catatan: handler `domain:overview` tetap `refreshDomainFromDisk` (re-parse penuh) per panggilan — itu pilihan desain dashboard live, di luar scope item ini.*
- [x] **WB-9 — Sort `getOverview` (DomainEngine): catatan tanpa `updated`/`date` (string kosong) terurut PALING ATAS** (dianggap terbaru) — sebaiknya paling bawah. · **Status:** ✅ 2026-08-10 (M4) — `byUpdated`: tanpa tanggal → turun ke bawah (keduanya kosong → 0); unit test 3 item.
- [x] **WB-10 — `updateNodeAndEdges` tidak mengonversi node attachment → note saat file di-rename .png → .md** (stale sampai rebuild penuh). · **Status:** ✅ 2026-08-10 (M6) — branch `existing.isAttachment` di `updateNodeAndEdges` mengonversi in-place (title/tags/aliases/path/type/outLinks, `isAttachment=false`) + hapus entri registry attachments. Defensif di engine (alur app sudah cleanup via watcher); test: attachment → note id collision.
- [x] **WB-11 — `inferKindFromName` (TemplateEngine) heuristik substring bisa salah klasifikasi** (body menyebut "project" di prosa). · **Status:** ✅ 2026-08-10 (M4) — klasifikasi: (1) `type:` dari frontmatter (blok `---` saja), (2) konvensi NAMA file, (3) fallback `custom`. Prosa body TIDAK lagi di-scan. Unit test: prosa "project" → custom; frontmatter type menang; nama file meeting.
- [x] **WB-12 — Skor FTS semua ≥ 80 → normalisasi hybrid memipihkan skor keyword jadi 1.0**; hasil semantic jarang mengungguli. Bukan bug — pertimbangkan tuning bobot. · **Status:** ✅ 2026-08-10 (M6) — `minMaxNormalize` dihapus; blend hybrid pakai skala ABSOLUT: keyword (FTS 0-100 / Fuse) × 0.6, semantic (cosine→0-100) × 0.4. Semantic kuat (≥ ~0.9) kini mengungguli tail keyword lemah; exact keyword tetap memimpin. Test: semantic 0.98 mendahului fuzzy hit lemah ('TypeScrpt' → fuse ≈ 0.78).
  - **Validasi vault nyata (2026-08-10):** harness `scripts/wb12-validation.ts` (`npx tsx`, env `WG_NO_SEARCH_WORKER=1`) — corpus 37 docs repo asli, FTS SQLite + Fuse asli (better-sqlite3), embedding MiniLM asli (`Xenova/all-MiniLM-L6-v2`, download sekali ~23MB ke node_modules cache). 6 query, OLD (min-max) vs NEW (produksi). Hasil: **perbaikan terkonfirmasi** — (Q2) keyword lemah skor 1.3 tidak lagi memuncaki hasil; (Q3) semantic marginal 0.51 tidak lagi melompat ke #2 di atas keyword 90+. **Cacat baru ketemu & diperbaiki:** (Q4/Q6) dedupe keyword-first membuang skor semantic untuk doc yang juga punya keyword hit lemah → doc paling relevan tenggelam (28_Plugin_SDK 'plugin sdk development' → posisi 5; 07_Search_Engine 'hybrid semantic search' → keluar top-5). Fix: **merge per-doc `max()`** — `semBoost` Map; doc di kedua daftar mempertahankan sinyal terbaiknya. Re-validasi: 28_Plugin_SDK → #1, 07_Search_Engine → #3. Test baru: weak-kw + strong-sem doc direscue ke #1.
  - Catatan: `src/test/setup.ts` me-mock `@xenova/transformers` + `better-sqlite3` (semua test), jadi validasi asli TIDAK bisa jalan di vitest — pakai tsx di luar vitest. Env guard `WG_NO_SEARCH_WORKER=1` (Fuse lokal, tanpa worker thread) ditambahkan di SearchEngine — berguna untuk debug/perf A/B di node polos (worker hanya tersedia di build electron-vite).

### Positif terverifikasi (tanpa aksi)

- `IndexDatabase`: FTS5 content-table + trigger delete/update benar; query di-escape aman (term dikutip + prefix); WAL + `synchronous=NORMAL`; DB korup di-quarantine → fallback Fuse-only, tidak pernah crash.
- `GraphLayoutStore`: normalisasi value (camera/settings/views), atomic write, fallback korup — solid.
- `GraphEngine`: lookup map (suffix path, alias, dedup ambiguous) Obsidian-like dan teruji; ghost/tag/attachment nodes konsisten; orphan = real note tanpa koneksi wiki.
- `TemplateEngine`: seeding idempotent; var tak dikenal → '' (tidak crash).
- `DomainEngine`: nomor baris checkbox → click-to-open; `openCheckboxes` di-overview dicap 50 tapi `counts` menghitung semua.

---

## Wave C — Temuan Audit Keamanan (2026-08-10)

> Hasil audit AUD-09..11 (`PathSandbox`, `SecretsStore`, `Permissions`). Sumber: `docs/AUDIT_PLAN.md`. **Tidak ada celah sandbox yang terbuka** — cakupan `assertPathInVault` terverifikasi menyeluruh. Sisa temuan: 1 keputusan postur keamanan (WC-4), 1 field mati (WC-5), dan beberapa hardening/NIT.

### P1 — Keputusan postur keamanan

- [x] **WC-4 — Semua permission default TERBUKA (`p.X !== false`) — termasuk plugins (eksekusi kode) & automation (aksi file).**
  - **File:** `src/main/security/Permissions.ts` (`readPermissions`, `DEFAULT_PERMISSIONS`)
  - **Masalah:** saat `settings.permissions` tidak ada (vault/instalasi baru), `aiAccess/aiTools/automation/plugins` semuanya `true`. Vault dari sumber tak tepercaya yang berisi `plugins/` akan auto-load plugin (eksekusi kode di main process), dan rule `automation.json` langsung aktif.
  - **Solusi (putuskan):** (a) default `plugins`/`automation` = OFF untuk instalasi baru; (b) atau pertahankan tapi pastikan UI Settings → Security menampilkan status dengan jelas saat pertama kali. Tidak ada kode yang perlu diubah untuk (a) selain nilai default + migrasi.
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 — keputusan (a), dikerjakan M1
  - **KEPUTUSAN (2026-08-10):** **(a) default OFF** untuk `plugins` & `automation` (`p.x === true` untuk ON); `aiAccess`/`aiTools` tetap ON (produk inti). Ubah `readPermissions` + `DEFAULT_PERMISSIONS`; Settings → Security tampilkan status postur saat pertama. Catatan: toggle di Settings harus TETAP bisa mengaktifkan (gate AE-3 jangan mengunci UI sendiri). Masuk M1.

### P2 / P3

- [x] **WC-5 — Field `aiAutoWrite` mati: dideklarasikan & dibaca tapi tidak pernah menjadi gate apa pun.**
  - **File:** `src/main/security/Permissions.ts` (line 14/22/32), `SettingsView.tsx` (line 164)
  - **Masalah:** komentar mengklaim "never true — always confirm", dan itu benar karena arsitektur (Apply selalu manual) — tapi field-nya menyesatkan; siapa pun yang membaca kode bisa mengira ada jalur auto-apply.
  - **Solusi:** hapus field (keamanan tetap dijamin arsitektur), atau wire dengan sengaja (tidak disarankan).
  - **Effort:** sangat kecil

- [x] **WC-6 — Tidak ada kontrak/guard yang mencegah handler mengembalikan `getSettings()` mentah (berisi key terdekripsi) ke renderer.**
  - **File:** semua handler (saat ini aman — terverifikasi; lihat Wave A)
  - **Masalah:** keamanan saat ini bergantung pada disiplin; satu handler baru yang `return workspaceEngine.getSettings()` akan membocorkan semua key.
  - **Solusi:** tambahkan assertion di `sourceContracts.test.ts`: tidak boleh ada `ipcMain.handle` yang me-return `workspaceEngine.getSettings()` tanpa melalui `scrubSettingsSecrets`.
  - **Effort:** kecil

- [x] **WC-7 — TOCTOU path sandbox: path dicek lalu dioperasikan; symlink bisa ditukar di antara keduanya.**
  - **File:** `src/main/security/PathSandbox.ts` (pola `assertPathInVault` lalu fs-op)
  - **Masalah:** risiko kecil untuk app lokal; hardening opsional.
  - **Solusi:** re-verifikasi realpath saat operasi (atau buka dengan `O_NOFOLLOW` untuk file write).
  - **Effort:** sedang · opsional · **Status:** ✅ 2026-08-10 (M4) — `reverifyPathInVault` (realpath saat ini vs vault) dipanggil di `WorkspaceEngine.writeFile/createFile/deleteFile/moveToTrash` — menutup jendela swap-symlink di semua jalur tulis (handlers + AgentTools + automation). Unit test: symlink di-swap dalam→luar → throw.

### Positif terverifikasi (tanpa aksi)

- **Cakupan sandbox menyeluruh**: `files.ts` (8 call site), `templates.ts`, `ai.ts` (3: sendMessage/streamMessage/streamPipeline), `AgentTools.ts` (5), `WorkspaceMemory.ts`, `InternalAPI.ts`, `AutomationEngine.ts` (2). Tidak ada handler file yang lolos tanpa `assertPathInVault`.
- **Path traversal chat/checkpoint tertutup**: `ConversationStore` & `CheckpointStore` men-sanitize id (`[^a-zA-Z0-9_-]` → `_`, cap 160) + cek `rel.startsWith('..')` sebelum tulis/baca.
- **`graph:savePng`** memakai native save dialog (path pilihan user, sengaja tidak di-vault-guard).
- **SecretsStore**: legacy plaintext otomatis ter-enkripsi pada save berikutnya; fallback `plain:` terekspos via `security:status`; scrub + merge-preserve + protect/reveal benar; tidak ada field secret lain di luar `settings.ai`.
- **Gate permissions**: `aiAccess`/`aiTools` di stream handlers; `automation` di automation handler + fileWatcher; `plugins` via `pluginHost.setAllowed` + plugins.ts; MCP tercakup `aiTools` di level stream.

---

## Wave D — Temuan Audit AI (2026-08-10)

> Hasil audit AUD-12..17 (provider layer, middleware, agent tools, konteks/memori, percakapan/checkpoint, logging/cost). Sumber: `docs/AUDIT_PLAN.md`. **Ini area terkuat di codebase** — tidak ada temuan menengah; sisanya hardening & nit. Yang penting: jalur abort/cancel terverifikasi bersih di semua provider.

### P3 / Hardening

- [x] **AD-1 — Tidak ada handler `unhandledRejection`/`uncaughtException` di main process.**
  - **File:** `src/main/index.ts` (cek saat ini tidak ada)
  - **Masalah:** semua jalur abort SAAT INI bersih (tiap provider `if (signal?.aborted) return` — resolve, bukan reject), tapi satu regresi di masa depan (provider baru yang reject setelah abort) bisa jadi rejection tak tertangani di main.
  - **Solusi:** tambahkan handler defensif `process.on('unhandledRejection')` / `uncaughtException` yang mencatat + tidak meng-crash sembarangan (atau minimal log).
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M3) — module `src/main/processSafety.ts` (`installProcessSafetyNet`, idempotent) dipasang di `main/index.ts` sebelum async work; log + tidak crash. Unit test: rejection/exception tidak crash, idempotent.

- [ ] **AD-2 — Dedup `parseToolActions` membuang SEMUA duplikat tool+args (global), sementara komentarnya bilang "consecutive identical".**
  - **File:** `src/main/ai/AgentTools.ts` (sekitar line 507)
  - **Masalah:** model memanggil tool yang sama dua kali dengan argumen sama (mis. membaca file yang sama untuk verifikasi) — panggilan kedua diam-diam dibuang. Untuk read biasanya diinginkan; untuk write, dua proposal identik → satu hilang.
  - **Solusi:** samakan perilaku dengan komentar (hanya consecutive) atau dokumentasikan dengan sengaja bahwa dedup global adalah fitur. Putuskan + perbaiki komentar/test.
  - **Effort:** kecil

- [x] **AD-3 — `loadPromptEntries` menulis `prompts.json` non-atomic** (crash mid-write → korup → tapi load toleran korup, jadi aman). Pakai `atomicWriteJson` untuk konsistensi dengan store lain.
  - **Effort:** sangat kecil

- [x] **AD-4 — `request.model` di-mutasi in-place di `runStreamInner`** (set default) — aman untuk pemakaian sekarang (caller tidak reuse), tapi mutasi argumen fungsi umumnya kurang bersih. NIT.
  - **Effort:** sangat kecil

### Positif terverifikasi (tanpa aksi)

- **Provider layer**: retry hanya 429/5xx dengan backoff+jitter (cap termasuk jitter); `shouldRetry` berhenti saat abort; model discovery timeout 8s + fallback static + cache TTL 5 mnt (stamp hanya saat live); failover murni & teruji (Ollama tak pernah kandidat; 4xx non-terminal tampil langsung); **semua provider (OpenAICompat/Claude/Gemini/Grok/Ollama) menangani abort dengan resolve** — tidak ada unhandled rejection pada cancel/watchdog.
- **Middleware**: watchdog per-round + guard total 180s; cancel flag + `AbortController` nyata; resume dari checkpoint (startRound, sengaja +1 round konservatif); kompaksi otomatis 80% dengan tool-pairing utuh; workspace context hanya round 0 (lean prompt round 1+ — hemat billing); read paralel batch 4 + write sekuensial; sub-agent di-gate plan mode; unknown tool → notice eksplisit (bukan skip senyap); verifikasi sitasi sebelum done; estimasi cost + token.
- **AgentTools**: sandbox konsisten (5 call site `isPathInVault`); validasi proposal (cap 500KB, unclosed frontmatter ditolak); `applyProposal` status terminal (pending → applied/rejected); resolve path relatif/absolut aman (PathSandbox, bukan naive startsWith).
- **contextCompaction / PromptRegistry / cost**: pure + teruji; override prompt version-gated (stale fork jatuh ke default); harga runtime OpenRouter menang atas tabel statis; fallback generic + free (Ollama).
- **ConversationStore/CheckpointStore**: id di-sanitize (`[^a-zA-Z0-9_-]` → `_`, cap 160) + rel-check `..` + atomic write + quarantine korup.
- **AIEventLog**: trail terstruktur, di-cover test (`AIEventLog.test.ts`, `cost.test.ts`).

---

## Wave E — Temuan Audit IPC & Preload (2026-08-10)

> Hasil audit AUD-18..21 (preload bridge, handler files/workspace/search/graph, handler ai/chat/checkpoint, platform mcp/plugins/automation/InternalAPI). Sumber: `docs/AUDIT_PLAN.md`. Satu temuan P2 nyata (AE-1 — index stale setelah rename); sisanya konsistensi & nit.

### P2 — Konsistensi data

- [x] **AE-1 — Rename file tidak me-re-sync file yang wikilink-nya ditulis ulang → graph/search menyimpan outLinks basi (ghost link) sampai rebuild penuh.**
  - **File:** `src/main/ipc/handlers/files.ts` (`file:rename`)
  - **Masalah:** `workspaceEngine.renameFile` menulis ulang `[[OldTitle]] → [[NewTitle]]` di SEMUA file yang terpengaruh di disk, lalu `markSelfWrite(f)` menekan echo chokidar — tapi tidak ada `syncSingleFile(f)` untuk file-file itu. Node graph-nya tetap memegang `outLinks` lama → resolusi jadi ghost `[[OldTitle]]` sampai vault di-reindex (reopen/rebuild). Hanya file yang di-rename yang di-sync.
  - **Solusi:** setelah rename, panggil `syncSingleFile(f, root)` untuk setiap `result.affectedFiles` (markSelfWrite sudah menekan echo; panggilan eksplisit tetap jalan).
  - **Effort:** kecil

### P3 / Nit

- [x] **AE-2 — Duplikasi ±25 baris teardown/setup vault antara `workspace:open` dan `workspace:create`** (clear engines, open db, syncWorkspaceData, attachFileWatcher, automation, pluginHost, mcp, embedding init). Risiko drift — ekstrak helper `openVaultFlow(root)` bersama. · **Status:** ✅ 2026-08-10 (M4) — `openVaultFlow(root)` di `workspace.ts` dipakai kedua handler. *Catatan perilaku: create kini juga menembak `workspace_opened` ke automation (sebelumnya hanya open) — unifikasi yang diinginkan (create = open juga).*
- [x] **AE-3 — `automation:setEnabled` tidak mengecek `perms.automation`** (tidak konsisten dengan `automation:save` & `automation:runRule` yang di-gate). Saat ini inert (fileWatcher double-check), tapi inkonsisten — tambahkan gate untuk konsistensi.
- [x] **AE-4 — `window.electron` (electronAPI) mengekspos raw `ipcRenderer` ke renderer** — pola standar electron-vite; renderer bisa invoke channel apa pun (typed API bersifat advisory, bukan batas keamanan). OK karena renderer tepercaya — dokumentasikan saja. · **Status:** ✅ 2026-08-10 (M4) — doc comment di `preload/index.d.ts` di atas deklarasi `Window` (postur: renderer tepercaya, proteksi vault hidup di main).
- [x] **AE-5 — `syncWorkspaceData` fire-and-forget saat open/create** — state kembali sebelum index selesai (pencarian segera setelah buka bisa parsial). Sengaja; pertimbangkan flag "indexing" di state bila mau. · **Status:** ✅ 2026-08-10 (M4) — `WorkspaceState.indexing?: boolean`; `openVaultFlow` set `true`, `syncWorkspaceData` selesai → `false` + push `workspace:updated`. Renderer bisa menampilkan indikator "indexing" dari flag ini (belum di-wire ke UI).
- [x] **AE-6 — `mcp:testServer` menulis+memulihkan config via `saveServers` dua kali** (write amplification + churn settings). Alternatif: test langsung tanpa persist. · **Status:** ✅ 2026-08-10 (M4) — `McpClientManager.testServer(server)`: daftar server di-set in-memory, connect, laporkan, restore — TANPA menulis mcp.json; handler `mcp:testServer` tinggal meneruskan.

### Positif terverifikasi (tanpa aksi)

- **Preload**: stream watchdog 200s + cleanup pada done/cancel/reject; tidak ada listener bocor; wrapper typed konsisten dengan handler (paritas channel di-cover `sourceContracts.test.ts`).
- **files.ts**: semua 8 operasi di-`assertPathInVault`; conflict detection mtime (theirs/yours); cap konten 5MB (write/create) & 500KB (render); `markdown:render` cap + frontmatter strip + try/catch; `file:openExternal` vault-sandboxed.
- **graph.ts**: `graph:savePng` via native dialog (path pilihan user, sengaja); layout via GraphLayoutStore (atomic + normalize).
- **plugins**: `plugins:runCommand` & `plugins:reload` di-gate `perms.plugins`; `security:status` hanya membaca raw TERENKRIPSI (tidak pernah kirim key).
- **ai/chat/checkpoint handlers**: sudah tercakup audit save-stop (MED-1..3) + Wave D — tidak ada temuan baru.
- **Paritas preload↔main**: 77+ channel dua arah dijamin `sourceContracts.test.ts` (kontrak statis).

---

## Wave F — Temuan Audit UI Renderer (2026-08-10)

> Hasil audit AUD-22..28 (editor, graph view, chat, settings, shell/dashboard, stores & hooks, styling). Sumber: `docs/AUDIT_PLAN.md`. **Catatan jujur**: store & komponen kunci (chatStore, editorStore, streamingMarkdown, MarkdownPreview, Dialog, SearchModal, AppShell) diaudit penuh; monolith GraphCanvas/SettingsView/ChatPanel hanya cross-check (sudah diaudit dalam di Wave D/E + save-stop). Tidak ada temuan menengah.

### P3 / Utang & nit

- [x] **AF-1 — GraphCanvas monolith (±4.3k baris, 204KB) + aksesibilitas graf minimal.**
  - **File:** `src/renderer/src/components/graph/GraphCanvas.tsx`
  - **Masalah:** hanya 16 atribut aria/role untuk canvas penuh; navigasi keyboard graf terbatas (zoom/pan via mouse); utang refactor sudah terdokumentasi di `docs/AUDIT.md`.
  - **Solusi:** pecah ke modul/hook; tambah fallback aksesibel (daftar node, kontrol zoom keyboard, `aria-label` per node saat focus). Perf sudah di-cover `graphPerf.test.ts` (LOD, hot set).
  - **Effort:** besar (paket refactor terpisah) · **Status:** ✅ 2026-08-10 (M5)
  - **Terslice (GraphCanvas 5.353 → ±4.800 baris, concern dipisah):**
    1. **`graphSvgItems.tsx`** (baru, render concern) — `SvgEdgeItem`/`SvgNodeItem`/`SvgLabelItem`/`PerfSparkChart` + `readThemeVar` keluar dari monolith; `SvgNodeItem` kini punya `aria-label` (title node — AF-1 a11y)
    2. **`graphPerfOverlay.ts`** (baru, perf concern) — hook `useGraphPerf(svgFrame, activeView)` menampung sampler commit + HUD interval + adaptive throttle; komponen cukup consume refs yang diekspos (`throttleWindowMsRef`, `perfCommitStartRef`, …)
    3. **Dedup helper ke `graphQuery.ts`** — `DEFAULT_DISPLAY_OPTS`/`matchGroupQuery`/`resolveGroupColors`/`lodLabel`/`applyForces`/`spiralSeed` yang selama ini diduplikasi DI DALAM GraphCanvas dihapus & di-import (single source); `textFade` diselaraskan ke 0.75 (keputusan G6; graphQuery masih 0.9 — disinkronkan)
  - **A11y dasar:** daftar node sr-only (fallback screen-reader/keyboard — 150 note teratas, tombol buka note), `aria-label` per node SVG, zoom buttons sudah ber-label (sudah ada), navigasi keyboard lengkap sudah ada (pan/zoom/[/]/Enter)
  - **Kontrak di-update:** 6 sourceContracts yang mengunci lokasi implementasi lama (perf sampler/throttle/spark/label anchor) kini menunjuk ke `graphPerfOverlay.ts`/`graphSvgItems.tsx` — konsisten dengan konvensi M1–M4

- [x] **AF-2 — `file:read` memakai `light:true` → wordCount 0 & headings [] di tab** — outline/statistik editor bergantung jalur lain; pastikan outline heading memakai jalur non-light (verifikasi saat menyentuh editor).
  - **Effort:** kecil · **Status:** ✅ 2026-08-10 (M2) — **TERVERIFIKASI TANPA PERUBAHAN KODE**: outline (`BacklinksPanel.tsx`) dan wordCount (`StatusBar.tsx`) menghitung dari `active.content` (konten tab mentah dari `file:read.content`), bukan dari field `headings`/`wordCount` hasil parse light. Jalur non-light tidak pernah dibutuhkan untuk statistik editor.

### Positif terverifikasi (tanpa aksi)

- **XSS renderer aman**: `streamingMarkdown` escape HTML PERTAMA (output AI = untrusted), link eksternal slot-protected (http/mailto), `data-target` escaped; `MarkdownPreview` hanya menerima HTML dari `markdown:render` main-process yang sudah di-escape (AUD-02).
- **chatStore**: tombstone `deletedChatIds` (save pasca-delete tidak membangkitkan file), `lastSavePromise` race delete-vs-save, checkpoint lifecycle (tulis saat truncate, hapus saat selesai), strip marker truncation, tool-run close-on-done. Exemplary.
- **editorStore**: `openTabChain` (serialisasi open), `pendingCloseWrites` (close-write vs reopen), `saveInflight` (save berantai), conflict merge dialog, per-tab debounce. Exemplary.
- **Dialog/SearchModal**: focus + Esc + `aria-modal` + `aria-activedescendant` + label. **AppShell**: semua hotkey butuh Ctrl/Cmd — tidak menelan ketikan biasa.
- **Styling**: `--color-*` di kedua tema, `:focus-visible` global, reduced-motion; CRLF/prettier debt sudah tercatat (AUD-30 nanti).

---

## Wave G — Temuan Audit Kualitas & Produk (2026-08-10)

> Hasil audit AUD-29..32 (QA suites, build/lint/perf, alur E2E, installer). Sumber: `docs/AUDIT_PLAN.md`. Verifikasi akhir: **1001/1001 test hijau** (60 file). Tidak ada temuan menengah.

### P3 / Nit

- [x] **AG-1 — `electron-builder.yml` tidak mengecualikan `.freebuff/` (DB + log dev) dan `.github/` dari paket asar.**
  - **File:** `electron-builder.yml` (`files:`)
  - **Masalah:** `.freebuff/desktop-v2.db` + log preview berpotensi ikut ter-package ke installer (bloat + artefak dev di produksi).
  - **Solusi:** tambahkan `!.freebuff/**`, `!.github/**`, `!perf-harness/**`, `!test-fixtures/**` ke daftar exclude.
  - **Effort:** sangat kecil

- [x] **AG-2 — Lint debt CRLF: 3342 warning `prettier/prettier` (Delete ␍)** — pre-existing, `npm run lint` gagal karena `--max-warnings 0`. Pilih kebijakan: (a) normalize seluruh repo ke LF (satu commit besar), (b) set `endOfLine: auto` di `.prettierrc.yaml` + biarkan CRLF, atau (c) terima sebagai non-blocker (dokumentasikan).
  - **Effort:** sedang (keputusan dulu)
  - **KEPUTUSAN (2026-08-10):** **(a) normalize ke LF** — setelah diukur, hanya **17 file** ber-CRLF (`src/main/ai/**`, `src/main/mcp/**`, handlers `mcp.ts`/`workspace.ts`, `qa/ipcHandlers.e2e.test.ts`) → satu commit kecil, bukan rombak repo. Tambah `.gitattributes` (`* text=auto eol=lf`) agar tidak kembali. *Menggantikan saran awal (b) — fakta baru: scope normalize jauh lebih kecil dari perkiraan.*
  - **✅ SELESAI (2026-08-10):** sisa **14 file** CRLF di-normalize ke LF (perl `s/\r$//`); `.gitattributes` di-update `* text=auto eol=lf` + pengecualian `*.bat text eol=crlf` (START-APP.bat tetap CRLF — parsing label/goto cmd.exe); binary (png/ico/icns) tidak tersentuh. Normalize tidak mengubah git diff (blob HEAD sudah LF semua; CRLF hanya artefak checkout autocrlf=true) — efeknya menyembuhkan lint + `eol=lf` mencegah CRLF balik lagi di checkout berikutnya. **Bonus:** setelah CRLF beres, lint masih gagal karena **57 warning pre-existing lain** yang tenggelam di bawah 3342 CRLF: 37 unused `eslint-disable` (no-console/no-require-imports di 7 file QA) + 20 `no-explicit-any` di `ipcHandlers.e2e.test.ts` — semua dibersihkan (directive dihapus via `--fix`; 20 `any` diketik ulang dengan `AIProviderDef` + tipe lokal `TestSettings`). **`npm run lint` kini exit=0 dengan `--max-warnings 0`.** Typecheck ✅ · 1043/1043 test ✅. Catatan teknis: setelah ubah `.gitattributes`, `git status` sempat menampilkan ~21 file sebagai phantom `M` (stat-cache basi; konten identik — diverifikasi via hash) — hilang setelah `git add -u && git reset` (tidak ada yang ter-stage).
  - **🛡️ CI guard (tambahan):** `scripts/check-crlf.mjs` + `npm run check:crlf` + job `crlf` di `.github/workflows/ci.yml` — scan SEMUA tracked text file (bukan hanya src/, karena lint hanya cover src/) untuk CRLF; skips `*.bat` (sengaja CRLF) + binary; gagal dengan pesan fix (`perl -pi -e 's/\r$//'`). Berjalan di tiap push/PR tanpa perlu `npm ci` (murni git+node). Detektor teruji: CRLF→FAIL, LF→OK.

### Positif terverifikasi (tanpa aksi)

- **QA suites exemplary**: `robustness.test.ts` (state korup → tidak pernah crash), `stressVault.test.ts` (ceiling generos anti-flaky, timer 60s hanya untuk deteksi hang), `resumeLive.test.ts` (alur resume nyata end-to-end), `ipcHandlers.e2e.test.ts` (mock electron + userData unik per-run). Script `test:flaky` (jalankan 2×) untuk deteksi flakiness.
- **Build**: `out/` 6.7MB, main bundle 436KB — wajar; `asarUnpack` better-sqlite3 + `.node` benar; NSIS oneClick=false + ganti direktori + icon lengkap (ico/icns/png + entitlements).
- **Perf**: `graphPerf.test.ts` meng-cover canvas2D, LOD, hot set, entry progress — dengan angka terukur.
- **Alur E2E**: preview browser membuktikan boot + render Welcome; alur penuh (dialog folder, safeStorage) butuh Electron — langkah manual terdokumentasi di `docs/FEATURE_AUDIT.md`.
- `notarize: false` untuk mac = keputusan terdokumentasi.
